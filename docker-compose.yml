version: '3'
services:
  node:
    build:
      context: .
    ports:
      - '3000:3000'
    volumes:
      - ./src:/var/www/src
    environment:
      - NODE_ENV=development
    tty: true
    container_name: react-playground
